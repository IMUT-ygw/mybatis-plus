知识点：
    1、test包下AutoGenerate.java自动生成代码（包含controller、service、mapper、xml、pojo类）
    2、 @TableField(fill = FieldFill.INSERT) 注解自动填充的特性，但是需要自定义新增数据策略类，这个类需要实现MetaObjectHandler类
    3、 @TableLogic(value = "0",delval = "1")逻辑删除，需要在数据库中设置字段，默认0不删除，1为删除，查询时数据库依旧存在，但是会过滤掉逻辑删除的数据